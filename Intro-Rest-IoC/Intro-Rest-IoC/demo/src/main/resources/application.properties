spring.application.name=demo
spring.h2.console.enabled=true

# Descomente para usar o banco de dados H2 (em memória) ao invés do MySQL
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.jpa.hibernate.ddl-auto=update

#Conexão com o MySQL - Substitua dw2 na URL pelo nome do seu banco
spring.datasource.url=jdbc:mysql://localhost:3306/trabalho11?serverTimezone=UTC
spring.datasource.username=ppi
spring.datasource.password=Ca123024975636.

# Driver do MySQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# O Hibernate irá criar as tabelas do MySQL automaticamente, caso não existam.
spring.jpa.hibernate.ddl-auto=update

# Exibe as instruções SQL executadas no console (útil para depuração)
spring.jpa.show-sql=true

# Mantem a conexão com o banco aberta até finalizar a requisição (view/JSON é gerado)
spring.jpa.open-in-view=true

